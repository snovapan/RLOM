<div class="col-12">
  <%= form_with(model: admin, local: true) do |form| %>
    <% if admin.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(admin.errors.count, "error") %> prohibited this admin from being saved:</h2>

        <ul>
        <% admin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="row row-admin">
      <%= form.label :name, "姓名",:class=>"col-4 text-right " %>
      <%= form.text_field :name, :class=>"col-6 input-adm" %>
    </div>

    <div class="row row-admin">
      <%= form.label :phone, "手机号",:class=>"col-4 text-right" %>
      <%= form.telephone_field :phone, :class=>"col-6 input-adm" %>
    </div>

    <div class="row row-admin">
      <%= form.label :password, "密码",:class=>"col-4 text-right" %>
      <%= form.password_field :password, :class=>"col-6 input-adm" %>
    </div>
<div class="newadmin-b mart-adm">
      <%= form.submit "创建", :class=>"btn btn-primary" %>
	  </div>
  <% end %>

</div>